<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Flight Management System</title>
  
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: rgb(52, 35, 211);
            color: white;
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            overflow: hidden;
        }
        .navbar {
            width: 100%;
            padding: 1rem;
            background-color: rgba(237, 227, 229, 0.8);
            border-radius: 1rem;
            text-align: center;
            margin-bottom: 2rem;
        }
        .navbar a {
            margin: 0.5rem;
            padding: 10px 15px;
            font-size: 1rem;
            font-weight: 500;
            text-decoration: none;
            color: white;
            background-color: #6a11cb;
            border-radius: 5px;
            transition: transform 0.3s, background-color 0.3s;
        }
        .navbar a:hover {
            background-color: #4b0e91;
            transform: scale(1.1);
        }
        .container {
            text-align: center;
            background-color: rgba(210, 37, 37, 0.1);
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
        }
        h1 {
            font-size: 2.5rem;
            margin-bottom: 30px;
            font-weight: 600;
            letter-spacing: 1px;
            animation: fadeInDown 1s;
        }
        .btn {
            margin: 10px;
            padding: 15px 25px;
            font-size: 1.1rem;
            font-weight: 500;
            border: none;
            border-radius: 5px;
            color: white;
            cursor: pointer;
            transition: transform 0.3s, background-color 0.3s;
        }
        .btn:hover {
            transform: scale(1.1);
        }
        .btn-primary { background: #080808; }
        .btn-primary:hover { background: #080808; }
        .btn-secondary { background: #0e0f0f; }
        .btn-secondary:hover { background: #165ecb; }
        .btn-success { background: #111111; }
        .btn-success:hover { background: #357a38; }
        @keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <a href="add_A" class="btn">Manage Airport</a>
        <a href="add_F" class="btn">Manage Flight</a>
        <a href="add_P" class="btn">Manage Passenger</a>
        <a href="add_T" class="btn">Manage Tickets</a>
        <a href="add_S" class="btn">Manage Staff</a>
        <a href="add_TR" class="btn">Manage Transactions</a>
        <a href="add_BAG" class="btn">Manage Baggage</a>
        <a href="add_H" class="btn">Manage History</a>
    </nav>
    <div class="container animate__animated animate__fadeInUp">
        <h1>Welcome to Flight Management System</h1>
        <div>
            <a href="/flights" class="btn btn-primary">View Flights</a>
            <a href="/passengers" class="btn btn-secondary">View Passengers</a>
            <a href="/tickets" class="btn btn-success">View Tickets</a>
            <a href="/staffs" class="btn btn-success">View Staff</a>
            <a href="/Airports" class="btn btn-success">View Airports</a>
        </div>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>
